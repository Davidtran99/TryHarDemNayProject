#
# Template cấu hình tunnel PostgreSQL. Sao chép file này thành
# `ops/.env.tunnel` (gitignore) rồi cập nhật thông tin thực tế.
#

HF_SPACE_ID=davidtran999/hue-portal-backend
NGROK_BIN=ngrok
NGROK_REGION=ap

# Local database trước khi forward
POSTGRES_HOST=localhost
POSTGRES_PORT=5543
POSTGRES_USER=hue
POSTGRES_PASSWORD=huepass123
POSTGRES_DB=hue_portal

# Các giá trị bên dưới sẽ được script `start_ngrok_and_set_db.py` cập nhật
# sau khi tạo tunnel thành công.
PG_TUNNEL_LOCAL_PORT=5543
PG_TUNNEL_HOST=
PG_TUNNEL_PORT=
PG_TUNNEL_USER=
PG_TUNNEL_PASSWORD=
PG_TUNNEL_DB=
PG_TUNNEL_LAST_UPDATED=
DATABASE_URL=

